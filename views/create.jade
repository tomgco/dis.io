h2 New Task
form.form-horizontal
  fieldset
    legend Basic Information

    .control-group
      label.control-label(for="taskName") Name
      .controls
        input(type="text").input-xlarge#taskName
        p.help-block The name of your task

    .control-group
      label.control-label(for="taskDescription") Description
      .controls
        textarea.input-xlarge#taskDescription(rows="3")
        p.help-block A description of your task

  fieldset
    legend Workunit Information

    .control-group
      label.control-label(for="workUnitJS") Work Unit
      .controls
        input.input-file#fileInput(type="file")
        p.help-block only .js files are valid

    .control-group
      label.control-label(for="taskPayload") Payload
      .controls
        textarea.input-xlarge#taskPayload(rows="5").
          {   'n': '1-1001337'
            , 'param2': [1,2,3,4]
            , 'param3': 99
          }
        p.help-block
          a(href='#') Load Help from side
            i.icon-question-sign(rel="popover", data-content="This determines how each task will be broken down into smaller pieces.", data-original-title="Payload?")
          | &nbsp;A JSON reprisentation of the Payload